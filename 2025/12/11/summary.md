# 2025年12月11日 每日总结


## 💡 心得感悟
      知识点：
          一、C语言：
          1、struct结构体访问内容，结构变量+‘.’+类型名称
          如果结构体变量中含有结构体，则访问格式为"结构体变量"+.+"结构体变量名"+.+类型名称
          ega:
          struct peo{
                char name [20]；
                char sex [50];
                int age ;
           }
           int main(){
                 struct peo p = {这里是初始化};
                 printf("%s %s %d",p.name,p.sex,p.age);
           return 0;     
            }
          2、使用指针对结构体变量进行访问，在指向类型时，用箭头“->”
          viod printf1 (struct peo*sp){
          printf("%s%s%d",sp->name,sp->sex,sp->age);
          }

          二、python：

          三、高数：

          四、汇编：

          五、linux指令：
          



      
      技巧点：
            一、C语言：

          二、python：

          三、高数：

          四、汇编：

          五、linux指令：
            

      



**自动创建时间：** Thu Dec 11 00:27:57 CST 2025
