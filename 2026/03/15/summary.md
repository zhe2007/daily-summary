# 2026年03月15日 每日总结


## 💡 心得感悟
      知识点：
          一、C语言：

          二、python：
          1、d.key返回所有键信息
          2,d.values返回所有值信息
          3,d.items返回所有键值对
          4,d.get(key,default)键存在返回相应值，否则返回default
            d.pop(key,default)键存在删除相应键值对，并返回相应值，否则返回default
            d.popitem()随机从字典中取出一个键值对，并以元组(key,value)形式返回，并将该键值对删除
            d.clear()清除字典d中所有键值对

          三、高数：

          四、汇编：

          五、linux指令：

          六、ctf:
          



      
      技巧点：
            一、C语言：

          二、python：

          三、高数：

          四、汇编：

          五、linux指令：

          六、ctf:

      



**自动创建时间：** Sun Mar 15 00:32:04 CST 2026
