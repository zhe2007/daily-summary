# 2026年03月19日 每日总结


## 💡 心得感悟
      知识点：
          一、C语言：

          二、python：
          1、turtle库
          前进 forward() 简称fd()
          后退 backward() 简称bk()
          left(angle)    right()
          pencolor(颜色或者(r,g,b))
          color()
          begin_fill()
          end_fill()
          penup() up()
          pendown down()
          circle(angle,step=)
          goto(,)移动到具体坐标
          2、random库
          sample(pop,k)从pop中选取k个值,以列表形式返回
          randrange(start,stop,step)
          3、jieba库，中文分词第三方库
          jieba.lcut(s)精确模式返回一个列表类型
          jieba.lcut(s,cut_all=Ture)全模式
          jieba.add_word()
          4、网络爬虫方向
          requests简单处理HTTP请求
          scrapy web获取框架，半成品爬虫
          5、数据分析
          numpy科学计算库
          scipy 再上个库的基础上增加函数
          pandas 数据处理、数据分析
          6、文本处理方向
          pdfminer 读取pdf数据
          openpyxl 处理excel表格
          python-docx 以此类推
          beautifulsoup4 解析和处理HTML、XML
          7、数据可视化
          matplotlib 二维图绘制
          TVTK 三维可视化
          mayavi 更方便的三维可视化

          三、高数：

          四、汇编：

          五、linux指令：

          六、ctf:
          



      
      技巧点：
            一、C语言：

          二、python：

          三、高数：

          四、汇编：

          五、linux指令：

          六、ctf:

      



**自动创建时间：** Thu Mar 19 01:00:23 CST 2026
